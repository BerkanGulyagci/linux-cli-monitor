#include <string>

namespace Config {
    const int CPU_DELAY_MS = 100;              // CPU ölçüm süresi (ms)
    const int TOP_PROCESS_COUNT = 5;           // Kaç işlem listelensin?
    const std::string REPORT_JSON = "report.json";
    const std::string REPORT_CSV = "report.csv";
}

